// PROPS => category
// use efect to generate array of locations (pass each location to CHILD)

// BODY
// category title?
// container for lcoation cards (not too much padding plz)

// CHILDREN
// loop through locations array and pass each location obj to a child
